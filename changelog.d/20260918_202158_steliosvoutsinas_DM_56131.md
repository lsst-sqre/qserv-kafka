### New features

- Accept an optional `adqlQuery` field on the `JobRun` Kafka message with the original ADQL query text. Send it through status messages and logging and forward it to Qserv as `adql_query`.
